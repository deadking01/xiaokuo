<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="com.git.wuqf"/>

    <context:property-placeholder location="classpath:config.properties"/>

    <!-- 配置服务注册组件 -->
    <bean id="registryFactory" class="com.git.wuqf.xiaokuo.registry.zookeeper.ZookeeperRegistryFactory">
        <constructor-arg name="zookeeperConnectionString" value="${registry.address}"/>
    </bean>

    <!-- 配置 RPC 服务器 -->
    <bean id="rpcServer" class="com.git.wuqf.framework.server.RpcServer">
        <constructor-arg name="serviceUrl" ref="serviceUrl"/>
        <constructor-arg name="registryUrl" ref="registryUrl"/>
        <constructor-arg name="registryFactory" ref="registryFactory"/>
    </bean>

    <bean id="registryUrl" class="com.git.wuqf.xiaokuo.common.URL">
        <constructor-arg name="url" value="${registry.address}"/>
    </bean>
    <bean id="serviceUrl" class="com.git.wuqf.xiaokuo.common.URL">
        <constructor-arg name="url" value="${server.address}"/>
    </bean>
</beans>